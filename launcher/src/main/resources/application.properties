spring.devtools.restart.enabled=false

junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=same_thread

it.tests-package=${INTEGRATION_TESTS_PACKAGE}
runner.logs-archive-directory=${ARCHIVE_DIRECTORY}
runner.kubernetes.config=${K8_CONFIG}
runner.kubernetes.dockerconfigjson=${DOCKER_CONFIG_JSON}
runner.kubernetes.namespace=${NAMESPACE}
runner.after-all.delete-namespace=true
kubernetes.external.access=.${runner.kubernetes.namespace}

spring.kafka.bootstrap-servers=kafka${kubernetes.external.access}:9093
spring.kafka.consumer.bootstrap-servers=kafka${kubernetes.external.access}:9093

logging.level.com.zaxxer.hikari.HikariConfig=OFF
logging.level.com.zaxxer.hikari=OFF
logging.level.org.apache.zookeeper=OFF
logging.level.org.apache.kafka=OFF

kubernetes.react-client.host=client${kubernetes.external.access}
kubernetes.react-client.port=3000
#
#kubernetes.aerospike.ip=aerospike${kubernetes.external.access}
#kubernetes.aerospike.port=3000
#
#kubernetes.zookeeper.ip=zookeeper${kubernetes.external.access}
#kubernetes.zookeeper.port=2181
#
#kubernetes.kafka.host=kafka${kubernetes.external.access}
#kubernetes.kafka.port=9093
#
#kubernetes.hollow.host=hollow${kubernetes.external.access}
#kubernetes.hollow.port=8082
#
#kubernetes.bidder-beacon.host=bidder-beacon${kubernetes.external.access}
#kubernetes.bidder-beacon.port=8080
#kubernetes.bidder-beacon.api=http://${kubernetes.bidder-beacon.host}:${kubernetes.bidder-beacon.port}
#
#kubernetes.minio.host=minio${kubernetes.external.access}
#kubernetes.minio.port=9000
#kubernetes.minio.username=minio
#kubernetes.minio.password=minio123
#kubernetes.minio.bucketnames=deepintent-hollow-itf-bidderbeacon,deepintent-hollow-itf-bidder,\
#  deepintent-hollow-itf-liveramp,deepintent-hollow-itf-matchbeacon,deepintent-hollow-itf-ssp

